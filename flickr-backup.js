(function(){
    var FlickrBackup = require('../flickr-backup.lib');

    // List files in folder

    // Check if not in history

    // Upload

    // Retry 3 times on error/no ID

    // Save to history
})();